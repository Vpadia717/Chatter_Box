<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChatterBox" parent="Theme.Material3.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/back_col</item>
        <item name="colorPrimaryVariant">@color/back_col</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_col</item>
        <item name="colorSecondaryVariant">@color/main_col</item>
        <item name="titleTextColor">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">true</item>
        <item name="android:navigationBarColor" tools:ignore="NewApi">@color/white</item>
        <item name="android:navigationBarDividerColor" tools:ignore="NewApi">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

</resources>